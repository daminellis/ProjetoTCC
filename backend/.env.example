# Copie este arquivo para `.env` e ajuste os valores.
# O `.env` real NÃO deve ser versionado (ver .gitignore).

# Banco de dados MySQL
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=3301
DB_NAME=tccdb

# Servidor Flask
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
# debug deve ficar como "false" em produção
FLASK_DEBUG=false
